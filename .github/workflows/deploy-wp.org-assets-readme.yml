name: Code checks
on:
  push:
    branches:
      - 'master'

  workflow_call:

  workflow_dispatch:

jobs:
  check:
    uses: publishpress/github-workflows/.github/workflows/deploy-free-assets.yml@v1
    secrets: inherit
