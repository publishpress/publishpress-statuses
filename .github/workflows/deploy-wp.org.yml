name: Deploy plugin to WordPress.org
on:
  release:
    types: [ released ]

jobs:
  code-check:
    name: Check the code
    uses: ./.github/workflows/code-check.yml
    secrets: inherit

  deploy-wp-org:
    # v2.0.0
    uses: publishpress/github-workflows/.github/workflows/deploy-free.yml@master
    needs: [code-check]
